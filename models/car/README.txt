Drop your car asset here using this exact filename:

car.fbx

Expected path in the app:
/public/models/car/car.fbx

Current loader behavior:
- FBX only (OBJ is intentionally not configured)
- The model is automatically scaled and centered
- If car.fbx is missing, a placeholder car is shown

Recommended export orientation from Blender:
- Forward axis: -Z
- Up axis: Y
